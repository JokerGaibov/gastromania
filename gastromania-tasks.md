# Gastromania: очередь задач

Рабочий файл для пошаговой доработки сайта. Источник требований: `gastromania-spec.md`.
Технический аудит и дизайн-система: `main.md`.

---

## Правила работы (соблюдать всегда)

1. **Одна задача за раз.** Не браться за следующую, пока текущая не принята.
2. **Перед началом задачи** перечитать соответствующий раздел `gastromania-spec.md`.
3. **После каждой задачи:** `npm run build` должен проходить без ошибок и без новых warning.
4. **После успешной сборки:** остановиться, коротко отчитаться что сделано и какие файлы затронуты, дождаться подтверждения. Не переходить к следующей задаче самостоятельно.
5. **Коммит** делается после подтверждения, отдельным коммитом на задачу.
6. **Не рефакторить лишнего.** Если по ходу видно проблему вне задачи, записать её в раздел «Найдено по ходу» внизу файла, но не чинить.
7. **Не выдумывать контент.** Если нужны реальные данные ресторана (адрес, телефон, цены, тексты), спросить, а не подставлять заглушки.
8. **Секреты.** `service_role` ключ Supabase не попадает в клиентский код и в репозиторий. В `NEXT_PUBLIC_*` только `anon` ключ. `.env.local` обязан быть в `.gitignore`.
9. Отмечать выполненные задачи галочкой в этом файле.

---

## Блок 1. Быстрые правки

Мелкие вещи, которые переживут любой рефакторинг. Можно делать подряд, отчёт после всего блока.

- [x] **1.1** Убрать неиспользуемый импорт `motion` в `Footer.tsx`
- [x] **1.2** Год копирайта в футере через `new Date().getFullYear()`
- [x] **1.3** Глобальная поддержка `prefers-reduced-motion` в CSS: отключить анимации и параллакс для тех, кто их не хочет
- [x] **1.4** Лайтбокс галереи: Escape закрывает, стрелки листают, фокус заперт внутри, `aria-modal="true"` и `role="dialog"`, возврат фокуса на миниатюру при закрытии
- [x] **1.5** Hero: `<img>` на `next/image` с `priority` и корректными `sizes` (в Next 16 `priority` заменён на `preload` — использован `preload`)
- [x] **1.6** Остальные `<img>` на `next/image`

**Готово когда:** сборка проходит, Lighthouse на десктопе показывает LCP лучше прежнего, лайтбокс полностью управляется с клавиатуры.

Намеренно не делаем: preconnect к Unsplash и починку мобильных Dishes. Оба компонента переписываются в блоке 4.

---

## Блок 2. Основа Supabase

- [x] **2.1** Установить `@supabase/supabase-js` и `@supabase/ssr`. Создать `lib/supabase/client.ts` (браузер) и `lib/supabase/server.ts` (серверные компоненты и route handlers). Добавить `.env.local` с `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY`, проверить `.gitignore`
- [x] **2.2** Написать миграцию со всеми таблицами из спеки: `profiles`, `menu_items`, `promotions`, `reservations`, `orders`, `favorites`, `delivery_settings`. Положить в `supabase/migrations/`
- [x] **2.3** Функция `is_admin()` и все RLS-политики из спеки. Отдельной миграцией
- [x] **2.4** Триггер на `auth.users`, создающий строку в `profiles` при регистрации
- [x] **2.5** Сгенерировать типы: `supabase gen types typescript` в `types/database.ts`

**Остановка — пройдена.** Миграции применены и проверены руками в Supabase Dashboard: тестовый пользователь создан, профиль появился автоматически, RLS проверена (чужие брони не видны, админ видит всё). Сгенерированные типы (`types/database.ts`) подтверждают все 7 таблиц и функцию `is_admin` в живой схеме.

---

## Блок 3. Авторизация

- [x] **3.1** Страница `/login`. Способ входа: email + пароль (владелец решил вопрос 1 из спеки в этом виде — телефон/SMS и magic-link не делаются)
- [x] **3.2** ~~`middleware.ts`~~ `proxy.ts` (переименован в Next.js 16): **только обновление сессии**, без редиректов — редиректы сделаны в layout, как и написано в этой же задаче ниже. Редирект не-админов с `/admin/*` на главную не сделан **специально** — по новому ТЗ владельца обычный пользователь видит экран «Доступ запрещён» на месте, а не перекидывается на главную (см. 3.3)
- [ ] **3.3** `app/account/layout.tsx` и `app/admin/layout.tsx` с проверкой на сервере — **только `app/admin/layout.tsx` сделан.** `app/account/layout.tsx` не делался — раздела `/account` ещё нет (Блок 8)
- [ ] **3.4** Кнопка входа и выхода в навигации, отображение имени пользователя — **не делалось**, вне рамок текущего запроса владельца. Кнопка «Выйти» пока есть только внутри `/admin`

**Блок 3 частично закрыт.** Готово: рабочий вход email+пароль, защита `/admin` (гость → `/login`, обычный пользователь → «Доступ запрещён», admin → доступ; роль `manager` из этого пункта убрана — см. финальную модель ролей admin/customer, main.md), временная страница `/admin` с именем/email/ролью/выходом. Не готово: `/account`, кнопка входа в общей навигации, редирект (вместо отказа) для обычных пользователей на `/admin`.

**Готово когда:** неавторизованный не попадает в `/account`, обычный пользователь не попадает в `/admin`, прямой переход по URL тоже блокируется. **Для `/admin` — подтверждено.** `/account` не существует, поэтому эта часть критерия неприменима, пока не сделан Блок 8.

---

## Блок 4. Меню из базы

- [ ] **4.1** Переписать `SignatureDishes.tsx` на чтение из `menu_items` где `category = 'signature'` и `is_active = true`. Заодно починить поведение на мобильных, которое было в старом P0-6
- [ ] **4.2** Страница `/menu`: полное меню, сгруппированное по категориям
- [ ] **4.3** Страница `/promotions`: активные акции с учётом `starts_at` и `ends_at`
- [ ] **4.4** Блок с актуальными акциями на главной
- [ ] **4.5** Настроить Supabase Storage для изображений блюд, перевести `next.config` с Unsplash на новый домен, добавить preconnect

**Остановка.** Перед этим блоком нужно реальное меню с ценами.

---

## Блок 5. Бронирование

- [x] **5.1** Форма брони пишет в `reservations` через server action. Валидация на сервере
- [x] **5.2** Чекбокс согласия на обработку персональных данных, обязательный, со ссылкой на `/privacy`. Момент согласия пишется в `consent_at`
- [x] **5.3** Состояния формы: отправка, успех, ошибка. Никаких молчаливых провалов
- [x] **5.4** Страница `/privacy` с политикой обработки персональных данных

**Блок 5 полностью закрыт и подтверждён владельцем.** Детали по задачам — в `main.md`, changelog v0.1.9–v0.1.11.

**Готово когда:** отправленная бронь видна в таблице `reservations` в Dashboard. **Подтверждено владельцем через Table Editor.**

---

## Блок 6. Доставка, заказы и онлайн-оплата

**Архитектура подготовлена (2026-09-11, вне очереди, по прямому запросу владельца) — см. `main.md` v0.1.17.** Схема `orders`/`order_items`, RLS, триггер `orders_guard_payment_status` (payment_status/paid_at меняет только серверный webhook, не обычный клиент) — готовы. Ниже — сама реализация, ещё не начата. Оплата — **только online** (`payment_method = 'online'`, наличные/карта курьеру не предусмотрены), способ доставки — курьер ресторана или агрегатор решается отдельно (см. «Решить до старта» в spec).

- [x] **6.1** Страница `/delivery`: меню на заказ с категорией `delivery`, добавление в корзину
- [x] **6.2** Корзина: количество, удаление, подсчёт суммы, сохранение между переходами по страницам (localStorage + React Context, `lib/cart/CartContext.tsx`)
- [x] **6.3** Оформление: имя, телефон, адрес, комментарий, согласие на обработку ПД. Работает и без регистрации
- [x] **6.4** Проверки перед отправкой: минимальная сумма (клиент — UX-подсказка, сервер — настоящая проверка внутри `create_order()`), доставка вкл/выкл. Часы работы кухни и зона доставки **не проверяются программно** — зоны только показываются информационно, геопроверка адреса не реализована (осознанно, см. main.md v0.1.18)
- [x] **6.5** ЗАКРЫТО. `create_order()` — security-definer RPC, единственный путь записи в `orders`/`order_items`. Прямой `insert with check (true)` убран. Возвращает `{order_id, order_number}` — человекочитаемый `order_number` (`#1001...`) добавлен отдельной миграцией, см. `main.md` v0.1.19
- [ ] **6.6** Выбор платёжного провайдера (ЮKassa/Точка/др.) — **отдельное решение владельца, требует его подтверждения перед подключением**
- [ ] **6.7** Создание платежа у провайдера после 6.5, редирект клиента на оплату
- [ ] **6.8** Route Handler — webhook провайдера: проверка подписи/валидности события, затем `payment_status='paid'` + `paid_at`. Это единственный путь, которому это разрешено — гарантировано триггером `orders_guard_payment_status` (`auth.role() = 'service_role'`), а не только соглашением на уровне приложения
- [x] **6.9** Экран подтверждения с номером заказа — сделан **временный** вариант («Заказ создан. Для завершения требуется онлайн-оплата», см. main.md v0.1.18), т.к. провайдера ещё нет. Настоящий экран «оплата подтверждена» — после 6.6–6.8
- [ ] **6.10** Подключить Telegram: Database Webhook на `orders` (`update`), fires when `payment_status` переходит в `'paid'`. Функция `telegram-notify` уже переписана под эту схему и сама подтягивает `order_items` (см. `supabase/functions/telegram-notify/index.ts`) — добавить только триггер, саму функцию переделывать не нужно. **Не подключать, пока нет провайдера — иначе неоплаченные заказы никогда не долетят до Telegram, а перепроверить это будет нечем**
- [x] **6.11** `/admin/orders`: список заказов с фильтрами по статусу, состав/суммы/оплата видны. Смена **только `order_status`** — при неоплаченном заказе доступна только отмена, весь остальной статус-флоу заблокирован в интерфейсе, пока `payment_status != 'paid'`

**Готово когда:** заказ появляется в `orders`+`order_items` с корректной суммой, при попытке подменить цену на клиенте сумма всё равно считается верно, `payment_status` никогда не становится `'paid'` иначе как через подтверждённый webhook, кухня не видит заказ раньше, чем он оплачен. **Всё, кроме самой оплаты (6.6–6.8, 6.10, финальный 6.9) — готово и проверено кроме живого прогона с реальными данными.**

---

## Блок 7. Админ-панель

Делать в этом порядке, от самого нужного персоналу к менее срочному. Панель обязана быть удобной с телефона.

- [ ] **7.1** `/admin/orders` — перенесено в Блок 6 (задача 6.11), т.к. зависит от готовой архитектуры оплаты того же блока. Не начато.
- [x] **7.2** `/admin/reservations`: список броней, смена статуса, фильтр по дате — сделано вне очереди (7.1 блокирован отсутствием Блока 6), по прямому запросу владельца
- [x] **7.3** `/admin/menu`: CRUD блюд, загрузка фото (Supabase Storage, бакет `menu-images`), **крупный заметный тумблер стоп-листа** для `is_active`
- [x] **7.4** `/admin/promotions`: CRUD акций (заголовок, описание, скидка %, даты действия, изображение, публикация)
- [x] **7.5** `/admin/delivery`: вкл/выкл, минимальная сумма, стоимость доставки, бесплатная доставка от суммы, часы кухни, районы доставки — одна запись настроек (`id=1`), без дублей
- [x] **7.6** `/admin` дашборд: новые брони, брони на сегодня, активные/стоп-лист позиции меню, активные акции, статус доставки + быстрые переходы
- [x] **7.7** `/admin/users`: список пользователей (имя/email/роль/дата), смена роли — **только role='admin'** может менять роли (доступные роли теперь только `admin`/`customer`, см. финальную модель ролей); самопонижение последнего admin'а заблокировано триггером в БД

**Готово когда:** с телефона можно снять блюдо со стоп-листа и перевести заказ в статус «доставлен» меньше чем за пять секунд. **Заказы (7.1) по-прежнему не сделаны — ждут Блок 6.**

---

## Блок 8. Личный кабинет посетителя

- [ ] **8.1** `/account`: обзор, последние брони и заказы
- [ ] **8.2** `/account/orders` и `/account/reservations`: полная история
- [ ] **8.3** `/account/favorites`: избранные блюда, кнопка добавления в избранное в меню
- [ ] **8.4** Редактирование профиля: имя, телефон, адрес по умолчанию для доставки

---

## Блок 9. Уведомления

- [x] **9.1** Telegram-бот, чат для персонала, токен и chat id в переменных окружения
- [x] **9.2** Supabase Edge Function, отправляющая сообщение в чат
- [ ] **9.3** Database Webhook на `insert` в `orders` и `reservations`, вызывающий функцию — **только `reservations` сделан и подтверждён живым тестом.** Триггер на `orders` не добавлен: таблица есть, но заказов ещё не существует (Блок 6 не начат) — нечем проверить.
- [ ] **9.4** Формат сообщения: состав, сумма, адрес, телефон, время. Читаемо с экрана телефона на кухне — формат брони (имя/телефон/время/гости/email/комментарий) готов и проверен. Формат заказа (`formatOrder` в `index.ts`) написан по спеке, но не проверен на реальных данных — ждёт Блок 6.

**Блок 9 частично закрыт.** Ветка reservations полностью работает и проверена в бою. Ветка orders — механизм готов (та же функция и та же схема триггера), но подключается вместе с Блоком 6, когда появятся реальные заказы для проверки.

**Готово когда:** тестовый заказ приходит в Telegram за несколько секунд.

---

## Блок 10. Подготовка к запуску

- [ ] **10.1** Метаданные: `title`, `description`, OG-изображение, фавикон
- [ ] **10.2** JSON-LD `Restaurant` с реальными адресом, часами и позициями меню из базы
- [ ] **10.3** `robots.txt` и `sitemap.xml`
- [ ] **10.4** Яндекс.Метрика
- [ ] **10.5** Яндекс.Карты в контактах
- [ ] **10.6** Реквизиты юрлица или ИП в футере
- [ ] **10.7** Error boundaries и страницы `not-found` и `error`
- [ ] **10.8** Прогон Lighthouse, цель по спеке из `main.md`
- [ ] **10.9** Проверка на реальных телефонах, iOS и Android, не только в DevTools
- [ ] **10.10** Домен, SSL, переменные окружения на Vercel

---

## Блок 11. После запуска

Полировка из старого Awwwards-роадмапа. Делается только когда структура перестала двигаться и сайт уже работает.

- [ ] Кастомный курсор
- [ ] Прелоадер и интро-анимация
- [ ] Активный индикатор секции в навигации
- [ ] Индикатор прогресса скролла
- [ ] Magnetic-кнопка
- [ ] Горизонтальный скролл для меню
- [ ] Вынос общих motion-утилит и UI-компонентов
- [ ] Английская версия

---

## Найдено по ходу

Сюда записывать замеченные проблемы, которые не относятся к текущей задаче. Разбирать отдельно.

- `Hero.tsx`: неиспользуемый импорт `useEffect` (предсуществующий, не введён Блоком 1). Даёт 1 ESLint warning.
- **Проекта нет в git вообще** (`git status` → not a git repository), и `.gitignore` отсутствовал — создал стандартный для Next.js при задаче 2.1. Правило «коммит после каждой задачи» из раздела «Правила работы» не может выполняться, пока нет репозитория. Нужно решение: `git init` делать мне, или сам?
- `npm install` на задаче 2.1 подсветил 5 high severity уязвимостей в транзитивных зависимостях (brace-expansion, js-yaml, next, postcss, sharp) — все предсуществующие, не от Supabase-пакетов. Фикс через `npm audit fix --force` поднимет Next.js до 16.2.12, вне заявленного диапазона версии — не делал, вне рамок задачи.
- **2026-08-09, вне очереди** (при точечном фиксе sticky в `SignatureDishes.tsx`, см. `main.md` v0.1.8): на мобильном при загрузке страницы обнаружено ~20px горизонтального скролла, не связанного с dishes-секцией (подтверждено — сдвиг остаётся даже при `display:none` на `#dishes`). Похоже на `x`-смещение анимации входа в какой-то другой секции (Story/Chef/Contact — везде похожий паттерн `initial={{ x: -N }}`), которое ничем локально не заклипано. Не чинил, вне рамок задачи — нужно найти источник и либо заклипать локально, либо проверить, действительно ли `body`'s `overflow-x:hidden` должен был это перехватывать (в тесте не перехватил: `window.scrollTo(100,0)` реально сдвигал `scrollX`).
- **2026-08-22, критично, вне очереди задачи 5.1** (детали в `main.md` v0.1.10): в `.env.local` были перепутаны значения — `NEXT_PUBLIC_SUPABASE_ANON_KEY` содержал похожий на secret-ключ Supabase (`sb_secret_...`, аналог `service_role`), опубликованный под `NEXT_PUBLIC_*`. Эти же (неверные) значения были скопированы на Vercel в задаче про деплой — то есть secret мог быть вшит в собранный клиентский бандл продакшена. Владелец перегенерировал ключи в Supabase Dashboard. Я обновил `.env.local` и переменные на Vercel (Production/Development), пересобрал прод через `vercel redeploy` (без выкладки неподтверждённой ветки). **Preview-переменные Vercel по-прежнему не выставлены** — CLI 53.1.0 отказывается добавлять их для «all Preview branches» даже по документированной команде (`git_branch_required` в non-interactive режиме). Не блокирует текущую работу, но preview-деплои веток `feature/*` не имеют рабочего Supabase, пока это не починено (возможно, поможет обновление CLI до 58.9.0).
- **2026-08-22, вне очереди задачи 5.1:** присланный владельцем project URL Supabase содержал опечатку (два переставленных символа в project ref) — не резолвился. Сверил с `supabase/.temp/linked-project.json` и живым HTTP-ответом (`401` вместо `000`), нашёл верный ref. Стоит иметь в виду при следующей работе с ключами — не доверять присланному значению без проверки на доступность.
